<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.material.lineteachin3">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="TeachinLine"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Iniciar_seccion" />
        <activity android:name=".Teacher" />
        <activity android:name=".Estudiante" />
        <activity
            android:name=".Main2Activity"
            android:label="TeachingLine"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity android:name=".Mostrarlista" />
        <activity android:name=".Detalle_Registro" />
        <activity
            android:name=".Navegacion"
            android:label="@string/title_activity_navegacion"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"></activity>
    </application>

</manifest>